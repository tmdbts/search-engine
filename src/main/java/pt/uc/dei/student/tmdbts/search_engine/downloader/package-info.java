/**
 * The downloader is responsible for downloading the data from the web and sending it to the search engine.
 * <p>
 * The Crawler is the manager of the downloaders. It creates the downloaders and assigns them the URLs to download.
 * It also receives the downloaded data and sends it to the barrels to be stored in the index.
 */
package pt.uc.dei.student.tmdbts.search_engine.downloader;